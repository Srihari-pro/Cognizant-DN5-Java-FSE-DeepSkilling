
# Git Hands-on 5 – Clean Up and Push to Remote Git

## Objective
Practice cleaning up a Git repository and working with a remote repository.

## Commands Executed

```bash
git status
git branch
git remote -v
```

## Result
- Verified the repository is in a clean state.
- Verified the current branch is `master`.
- Checked the configured remote repositories.
